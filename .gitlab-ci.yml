image: node:22

pages:
  stage: deploy
  script:
    - npm install  # 安装依赖
    - npm run build  # 构建项目（输出到 dist 目录，根据框架调整）
    - mkdir -p public
    - cp -r dist/* public/  # 将构建结果复制到 public 目录
  artifacts:
    paths:
      - public
  only:
    - master